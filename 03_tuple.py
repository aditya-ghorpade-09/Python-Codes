tup = (4,3,6,8,5,9)

# tuples are same as lists
# the only differnce is tuples are immutable
# and the lists are mutable

print(tup)

# if you want ot enter only one element in tuple you have to give at least a comma-
# tup = (1,)